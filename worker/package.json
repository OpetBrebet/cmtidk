{
	"name": "worker",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"db:generate": "drizzle-kit generate",
		"db:migrate": "wrangler d1 migrations apply cmtidk-db --local",
		"db:migrate:prod": "wrangler d1 migrations apply cmtidk-db --remote",
		"db:studio": "drizzle-kit studio",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"arctic": "^3.7.0",
		"drizzle-orm": "^0.45.2",
		"hono": "^4.12.12",
		"nanoid": "^5.1.7"
	},
	"devDependencies": {
		"@types/node": "^25.6.0",
		"drizzle-kit": "^0.31.10",
		"wrangler": "^4.81.1"
	}
}
